/**
 * 目前没用到（此文件作用于 `vite.config.js` 的 `optimizeDeps` 依赖预构建配置项）
 */
export default {
  //  预构建中自定义包含的依赖项（默认vite会自动扫描需要预构建的依赖项，也可自定义）
  include: [
    // 'element-plus',
    // 'axios',
    // 'pinia',
    // 'js-cookie',
    // '@vueuse/core'
  ],
  // 预构建中强制排除的依赖项
  exclude: []
}
